#include <bits/stdc++.h>
#include <vector>


using namespace std;
int main(){
  ios::sync_with_stdio(0); cin.tie(0);
  vector<pair<int, int>> a;
  int n;
  for (int i = 0; i <= n; i++){
    int x, y;
    cin >> x >> y;
    a.push_back({x, y});
  }
}
